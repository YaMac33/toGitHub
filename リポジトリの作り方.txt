リポジトリの作り方

1. フォルダを作成する

2. 作成したフォルダに移動して、Gitリポジトリを初期化する
   git init

3. GitHubに対象のリポジトリを作成して、リポジトリのURLをコピーする

4. コピーしたURLをoriginとして登録する
   git remote add origin URL

5. ファイルをステージングする
   git add .

6. コミットする
   git commit -m "作成"
